version: 1
frontend:
  phases:
    preBuild:
      commands:
        - python3 -m pip install -r requirements.txt
    build:
      commands:
        - python3 tools/build_all.py
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
